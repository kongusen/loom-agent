{
  "common_patterns": [
    {
      "use_case": "Invoice Processing",
      "description": "Extract invoice number, date, amount, and line items from invoice PDFs",
      "approach": "Use pdfplumber for table extraction, regex patterns for key fields"
    },
    {
      "use_case": "Resume Parsing",
      "description": "Extract contact info, work experience, education from resume PDFs",
      "approach": "Use PyPDF2 for text extraction, NLP for section identification"
    },
    {
      "use_case": "Contract Analysis",
      "description": "Extract key terms, dates, parties from legal contracts",
      "approach": "Extract text with PyPDF2, use keyword search and pattern matching"
    },
    {
      "use_case": "Report Summarization",
      "description": "Summarize multi-page PDF reports",
      "approach": "Extract text by sections, use LLM for summarization"
    }
  ],
  "code_snippets": {
    "extract_by_page_range": {
      "description": "Extract text from specific page range",
      "code": "pages = reader.pages[start:end]\ntext = ''.join([p.extract_text() for p in pages])"
    },
    "search_text": {
      "description": "Search for specific text in PDF",
      "code": "for i, page in enumerate(reader.pages):\n    text = page.extract_text()\n    if search_term in text:\n        print(f'Found on page {i+1}')"
    }
  }
}
